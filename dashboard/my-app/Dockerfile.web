# WEB: serve the built dashboard (./dist) via Nginx
FROM nginx:alpine

# Replace default site with our reverse-proxy setup (/api -> API service)
COPY nginx.conf /etc/nginx/conf.d/default.conf

# Copy the prebuilt frontend (dist) into the nginx web root
COPY dist /usr/share/nginx/html

EXPOSE 80
